# Pinned runtime dependencies for the BankIQ backend.
# Mirrors the [project.dependencies] table in pyproject.toml.
fastapi>=0.111
uvicorn[standard]>=0.30
sse-starlette>=2.1
pydantic>=2.7
pydantic-settings>=2.3
pandas>=2.2
numpy>=1.26
tabulate>=0.9
openai>=1.40
python-dotenv>=1.0
rich>=13.7
